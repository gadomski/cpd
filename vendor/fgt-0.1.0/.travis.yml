language: cpp

compiler:
    - clang
    - gcc

install:
    - make -f make/dependencies.mak install-travis

before_script:
    - if [[ $CXX == "g++" ]]; then
      export CXX="g++-4.8";
      fi

script:
    - make -f make/travis.mak build test install
